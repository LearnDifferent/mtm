<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.github.learndifferent.mtm.mapper.WebsiteMapper">
    <resultMap id="BaseResultMap" type="com.github.learndifferent.mtm.entity.WebsiteDO">
        <id column="web_id" jdbcType="INTEGER" property="webId"/>
        <result column="user_name" jdbcType="VARCHAR" property="userName"/>
        <result column="title" jdbcType="VARCHAR" property="title"/>
        <result column="url" jdbcType="VARCHAR" property="url"/>
        <result column="img" jdbcType="VARCHAR" property="img"/>
        <result column="desc" jdbcType="VARCHAR" property="desc"/>
        <result column="creation_time" jdbcType="DATE" property="createTime"/>
        <result column="public" jdbcType="BOOLEAN" property="isPublic"/>
    </resultMap>

    <select id="findPublicWebDataByFilter" resultMap="BaseResultMap"
            parameterType="com.github.learndifferent.mtm.query.WebFilterRequest">
        select *
        from website
        <where>
            `public` is true
            <if test="usernames != null and usernames.size() != 0">
                <foreach collection="usernames" item="userName" open="and (" close=")" separator="or">
                    user_name = #{userName}
                </foreach>
            </if>
        </where>
        <!-- fromDate 和 toDate 表示日期范围 -->
        <if test="fromDate != null and toDate != null">
            HAVING creation_time <![CDATA[ >= ]]> #{fromDate,jdbcType=DATE}
            AND creation_time <![CDATA[ <= ]]> #{toDate,jdbcType=DATE}
        </if>
        <!-- 如果上面不是「大于等于 / 小于等于」，而是「大于 / 小于」的情况
         ，上面的 if 标签需要加上「and !fromDate.equals(toDate)」
         ，然后还要加上下面这个 if 判断-->
        <!--        <if test="fromDate != null and toDate != null and fromDate.equals(toDate)">-->
        <!--            HAVING creation_time = #{fromDate,jdbcType=DATE}-->
        <!--        </if>-->
        <if test="order != null and order != ''">
            <!-- 动态 SQL 中，如果有 order by 的情况下，不要使用 # 符号，而要使用 $ 符号 -->
            order by ${order}
        </if>
        <if test="order != null and order != '' and desc != null and desc != false">
            desc
        </if>
        <if test="load != null and load > 0">
            limit 0, #{load}
        </if>
    </select>

    <select id="mostPublicMarkedWebs" resultType="com.github.learndifferent.mtm.dto.WebsiteWithCountDTO">
        select count(url) as count, url, title, `desc`, img
        from website
        where `public` is true
        group by url, title, `desc`, img
        order by count desc
        limit #{from}, #{size};
    </select>

    <select id="countDistinctPublicUrl" resultType="java.lang.Integer">
        select count(distinct url)
        from website
        where `public` is true;
    </select>

    <select id="getAllPubAndSpecUserPriWebs" resultMap="BaseResultMap">
        <!-- 因为 web_id 也是根据时间顺序生成的，所以用 creation_time 和 web_id 排序是一样的效果 -->
        select * from website
        <where>
            `public` is true
            <if test="specUsername != null and specUsername != ''">
                or user_name = #{specUsername}
            </if>
        </where>
        order by `web_id` desc
        <if test="from!=null and size!=null">
            limit #{from},#{size}
        </if>
    </select>

    <select id="getWebsiteDataById" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        select *
        from website
        where web_id = #{webId,jdbcType=INTEGER}
    </select>

    <select id="findWebsitesDataByUrl" resultMap="BaseResultMap">
        select *
        from website
        where url = #{url};
    </select>

    <select id="countAllPubAndSpecUserPriWebs" resultType="java.lang.Integer">
        select count(web_id)
        from website
        <where>
            `public` is true
            <if test="specUsername != null and specUsername != ''">
                or user_name = #{specUsername}
            </if>
        </where>
        ;
    </select>

    <select id="findWebsitesDataByUser" resultMap="BaseResultMap">
        select *
        from website
        where user_name = #{userName}
        <!-- 如果 includePrivate 为 false，就只查找数据库中 public 属性为 true 的 -->
        <if test="!includePrivate ">
            and `public` is true
        </if>
        order by web_id desc
        <if test="from!=null and size!=null">
            limit #{from},#{size}
        </if>

    </select>

    <select id="countUserPost" resultType="java.lang.Integer">
        select count(user_name)
        from website
        where user_name = #{userName}
        <if test="!includePrivate ">
            and `public` is true
        </if>
        ;
    </select>

    <select id="getAllPublicWebDataForSearch" resultType="com.github.learndifferent.mtm.dto.search.WebForSearchDTO">
        select url, title, img, `desc`
        from website
        where `public` is true
        group by url, title, `desc`, img;
    </select>

    <select id="findWebsitesDataExcludeUser" resultMap="BaseResultMap">
        select *
        from website
        where user_name != #{excludeUsername}
        and ( `public` is true
        <if test="userNameToShowAll != null and userNameToShowAll != '' and !userNameToShowAll.equalsIgnoreCase(excludeUsername)">
            or user_name = #{userNameToShowAll}
        </if>
        ) order by web_id desc limit #{from}, #{size};
    </select>

    <select id="countExcludeUserPost" resultType="java.lang.Integer">
        select count(web_id)
        from website
        where user_name != #{excludeUsername}
        and ( `public` is true
        <if test="userNameToShowAll != null and userNameToShowAll != '' and !userNameToShowAll.equalsIgnoreCase(excludeUsername)">
            or user_name = #{userNameToShowAll}
        </if>
        );
    </select>

    <delete id="deleteWebsiteDataById" parameterType="java.lang.Integer">
        delete
        from website
        where web_id = #{webId,jdbcType=INTEGER}
    </delete>

    <delete id="deleteWebsiteDataByUsername">
        delete
        from website
        where user_name = #{userName}
    </delete>

    <insert id="addWebsiteData" keyColumn="web_id" keyProperty="webId"
            parameterType="com.github.learndifferent.mtm.entity.WebsiteDO"
            useGeneratedKeys="true">
        insert into website(user_name, title, url, img, `desc`, `creation_time`, `public`)
        values (#{userName}, #{title}, #{url}, #{img}, #{desc}, #{createTime}, #{isPublic})
    </insert>

    <update id="updateWebsiteDataById" parameterType="com.github.learndifferent.mtm.entity.WebsiteDO">
        update website
        set user_name       = #{userName,jdbcType=VARCHAR},
            title           = #{title,jdbcType=VARCHAR},
            url             = #{url,jdbcType=VARCHAR},
            img             = #{img,jdbcType=VARCHAR},
            `desc`          = #{desc,jdbcType=VARCHAR},
            `creation_time` = #{createTime,jdbcType=DATE},
            `public`        = #{isPublic,jdbcType=BOOLEAN}
        where web_id = #{webId,jdbcType=INTEGER}
    </update>

    <select id="getUsernameByWebId" resultType="java.lang.String">
        select user_name
        from website
        where web_id = #{webId,jdbcType=INTEGER};
    </select>
</mapper>